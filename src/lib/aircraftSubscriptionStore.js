/**
 * @deprecated La persistencia de aeronaves, planes y suscripciones vive en el API Laravel.
 * Solo se mantiene la utilidad de contexto de proveedor para compatibilidad de imports.
 */
export { resolveProviderIdForUser } from './providerContext'
